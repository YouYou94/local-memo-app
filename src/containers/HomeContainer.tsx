import { Clock, Search, Template } from '../components';

const HomeContainer = () => {
  return (
    <Template>
      <Clock />
      <Search />
    </Template>
  );
};

export default HomeContainer;
