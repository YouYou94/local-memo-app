import { Preview, Template, Write } from '../../components';

const HomeContainer = () => {
  return (
    <Template>
      <Write />
      <Preview />
    </Template>
  );
};

export default HomeContainer;
