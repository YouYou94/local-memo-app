import { Template, Enrol } from '../../components';

const HomeContainer = () => {
  return (
    <Template direction={'column'}>
      <Enrol />
    </Template>
  );
};

export default HomeContainer;
